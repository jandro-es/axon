import AxonKit
import SwiftUI

/// Replaced by the paged wizard in Task 13.
struct OnboardingWindow: View {
    var body: some View {
        WindowPlaceholder(
            title: "Welcome to Axon",
            systemImage: "sparkles",
            detail: "The guided first-run prerequisite walkthrough arrives here."
        )
    }
}
